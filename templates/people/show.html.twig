{% extends 'base.html.twig' %}

{% block title %}People{% endblock %}

{% block body %}
    <h1>People</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ person.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ person.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('people_index') }}">back to list</a>

    <a href="{{ path('people_edit', {'id': person.id}) }}">edit</a>

    {{ include('people/_delete_form.html.twig') }}
{% endblock %}
